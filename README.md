# IDEAGINE 颜色工具站

这是一个基于 VitePress 构建的静态工具站点，用于提取图片主题色。

---

## 📚 GitHub 版本管理完全指南

本指南将教你如何使用 GitHub 来保存代码历史、管理版本，并为自动化部署做准备。

### 1. 安装 Git
（如果您已经安装，请跳过此步）
- **下载**：访问 [git-scm.com](https://git-scm.com/) 下载并安装 Windows 版本。
- **验证**：在终端输入 `git --version`，出现版本号即为成功。

### 2. 初始化与文件过滤
Git 通过一个名为 `.gitignore` 的文件来决定**不上传**哪些文件。
我已经为您创建了这个文件，它会自动忽略：
- `node_modules/` (项目依赖，体积巨大，不应上传)
- `docs/.vitepress/dist/` (构建生成的网页，不需要上传源码库)
- `docs/.vitepress/cache/` (缓存文件)

**您只需要执行以下命令，Git 会自动根据规则只上传代码文件：**

```powershell
# 1. 初始化 Git 仓库
git init

# 2. 添加所有文件（Git 会自动忽略 .gitignore 中指定的文件）
git add .

# 3. 提交更改
git commit -m "Initial commit"
```

### 3. 推送到 GitHub

1.  **在 GitHub 上创建新仓库**:
    *   登录 GitHub，点击右上角的 `+` -> **New repository**。
    *   **Repository name**: 输入项目名，例如 `ideagine-colors`。
    *   **Public/Private**: 选择 Public (公开) 或 Private (私有) 均可。
    *   **Initialize this repository with...**: **不要**勾选任何选项（因为我们本地已经有代码了）。
    *   点击 **Create repository**。

2.  **关联远程仓库并推送**:
    在 GitHub 创建成功后的页面上，你会看到一段代码，找到 **“…or push an existing repository from the command line”** 这一栏。
    
    复制那三行命令，在你的 VS Code 终端里粘贴并运行：

    ```bash
    # 1. 关联远程地址 (将 URL 换成你刚才创建的仓库地址)
    git remote add origin https://github.com/你的用户名/ideagine-colors.git

    # 2. 重命名主分支为 main (规范做法)
    git branch -M main

    # 3. 推送到 GitHub
    git push -u origin main
    ```

    *如果是第一次连接，可能会弹出窗口让你登录 GitHub 账号授权。*

---

### 🔄 日常开发与更新流程

以后每次修改完代码（比如修了个 Bug 或加了新功能），只需执行以下三步：

1.  **保存 (Add)**:
    ```bash
    git add .
    ```

2.  **提交 (Commit)**:
    ```bash
    git commit -m "这里写你改了什么，比如：优化颜色算法"
    ```

3.  **推送 (Push)**:
    ```bash
    git push
    ```
    *执行完这一步，GitHub 上的代码就和本地同步了。*

---

## ☁️ 自动化部署 (GitHub + Cloudflare Pages)

这是最推荐的部署方式。现在的架构是：**本地代码 -> GitHub 仓库 -> Cloudflare Pages 自动构建发布**。
一旦配置完成，以后您只需要在本地执行 `git push`，Cloudflare 就会自动拉取最新代码并更新网站。

### 步骤：

1.  **前提条件**：确保您已经按照上文指南，将代码成功推送到 **GitHub**。
2.  登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)。
3.  在左侧菜单点击 **Workers & Pages** -> **Create application** -> **Pages** -> **Connect to Git**。
4.  登录您的 GitHub 账号并授权 Cloudflare 访问。
5.  **选择仓库**：在列表中选中您刚才创建的 `ideagine-colors` 仓库，点击 **Begin setup**。
6.  **配置构建参数 (关键步骤)**:
    *   **Project name**: 保持默认或修改为您喜欢的（这将决定您的默认域名，如 `project-name.pages.dev`）。
    *   **Framework preset**: 在下拉菜单中找到并选择 `VitePress` (Cloudflare 会自动填充后续两项)。
    *   *确认 Build command 为 `npm run build`*
    *   *确认 Build output directory 为 `docs/.vitepress/dist`*
7.  点击 **Save and Deploy**。

等待几分钟构建完成后，Cloudflare 会给您一个访问链接（例如 `https://ideagine-colors.pages.dev`）。以后您每次推送代码到 GitHub，这个链接的内容都会自动更新。

---

## 🛠️ 本地开发命令

- **启动开发服务器**:
  ```bash
  npm run dev
  ```
  访问: `http://localhost:5173`

- **构建生产版本**:
  ```bash
  npm run build
  ```
